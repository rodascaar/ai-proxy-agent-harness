# Configuración de golangci-lint para el port Go de atomic_ai.
version: "2"

run:
  timeout: 3m

linters:
  default: standard
  enable:
    - revive
    - errcheck
    - ineffassign
    - staticcheck
    - unused
    - govet
    - inamedparam
  settings:
    revive:
      rules:
        - name: exported
          disabled: false

formatters:
  enable:
    - gofmt
    - goimports